export interface SectionFive {
  id: number;
  need: number[];
  reason: any[];
  answers: Array<Array<number>>;
  riskCapacity: number[];
  riskAttitude: number[];
  issues: any[];
  status: number;
  editableStatus?: number;
}
