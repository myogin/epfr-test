export interface SectionFive {
  id: number;
  need: boolean[];
  reason: any[];
  answers: Array<Array<number>>;
  riskCapacity: number[];
  riskAttitude: number[];
  issues: any[];
  status: number;
}
